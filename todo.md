# TODO

이 문서는 폴더 내 문서들과 현재 앱 구현을 비교해서, 아직 구현되지 않았거나 문서 요구와 다르게 동작하는 부분을 앱 진행 순서에 맞춰 정리한 목록입니다.  
중요도 순이 아니라 `ASRS -> DSM-5 -> 반응성 테스트 -> report -> 공통 구조` 순으로 정렬했습니다.

## 1. ASRS

### 1-1. ASRS 전용 결과 프롬프트의 `CBT 기반 실천 전략 3가지` 출력 구조 미구현

- 문서 기준:
  - [asrs/ASRS 설문평가방식.txt](/home/sqmini/soulai/asrs/ASRS%20설문평가방식.txt)
- 문서 요구:
  - ASRS 결과 직후
    - 결과 요약
    - 심리학적 해석
    - CBT 기반 실천 전략 3가지
    - 주의사항 및 상담 권유
  - 이 4개 블록 구조로 출력
- 현재 구현:
  - `summary`, `attention`, `hyperactivity`, `guidance` 중심의 짧은 quick analysis만 있음
  - ASRS 단계에서 별도의 `실천 전략 3가지`는 생성하지 않음
- TODO:
  - ASRS quick analysis를 현재처럼 짧게 유지할지
  - 문서 기준으로 전략 3개까지 확장할지 결정

### 1-2. ASRS 문항별 설명 툴팁/영역 안내는 아직 미구현

- 문서 기준:
  - [asrs/ASRS질문.txt](/home/sqmini/soulai/asrs/ASRS질문.txt)
- 문서 요구:
  - 예시 보기 구조는 현재 반영됨
  - 추가로 문항이 무엇을 보는지 짧은 설명 툴팁을 붙일 수 있게 제안
- 현재 구현:
  - 예시 토글은 있음
  - `이 문항이 보는 것: 부주의/충동성` 같은 짧은 영역 설명은 없음
- TODO:
  - 예시 아래에 영역 라벨이나 툴팁을 추가할지 검토

### 1-3. ASRS 문서의 사용자군별 예시 분기(학생/직장인/주부)는 미구현

- 문서 기준:
  - [asrs/ASRS질문.txt](/home/sqmini/soulai/asrs/ASRS질문.txt)
- 문서 요구:
  - 사용자군에 따라 예시 문구를 나눌 수 있도록 확장 가능하다고 제안
- 현재 구현:
  - 공통 예시만 사용
- TODO:
  - 사용자군 분기까지 갈지 판단

## 2. DSM-5

### 2-1. DSM-5 시작 안내 문구 구조 미구현

- 문서 기준:
  - [dsm5/1. DSM-5 기반 yesno 설문 만들기.txt](/home/sqmini/soulai/dsm5/1.%20DSM-5%20기반%20yesno%20설문%20만들기.txt)
- 문서 요구:
  - 검사 시작 전에
    - 선별 도구라는 점
    - 최근 6개월 기준
    - 정답/오답 없음
    - 3~5분 소요
    - 전문가 평가 필요
  - 를 따로 안내
- 현재 구현:
  - 질문 화면 설명은 있지만 별도의 전용 시작 안내 블록은 없음
- TODO:
  - DSM 시작 전 안내 카드 추가 여부 검토

### 2-2. DSM-5 문서가 아동/청소년 표현 기준인데 현재 앱은 성인용 흐름과 혼재

- 문서 기준:
  - [dsm5/1. DSM-5 기반 yesno 설문 만들기.txt](/home/sqmini/soulai/dsm5/1.%20DSM-5%20기반%20yesno%20설문%20만들기.txt)
- 문서 요구:
  - 문구는 아동(6~12세) / 청소년(13~17세) 평가 톤
- 현재 구현:
  - 앱 전체는 성인 ADHD 흐름과 맞춰져 있음
  - report와 ASRS도 성인 기준
- TODO:
  - DSM 문서를 성인용으로 다시 정리할지
  - 앱에 연령 버전을 분기할지 결정

### 2-3. DSM-5 결과 해석용 AI 프롬프트의 세부 유형 문안은 일부만 반영

- 문서 기준:
  - [dsm5/2. DSM-5 설문 평가 방식_3. DSM-5 결과를 해석하기 위한 AI 프롬프트.txt](/home/sqmini/soulai/dsm5/2.%20DSM-5%20설문%20평가%20방식_3.%20DSM-5%20결과를%20해석하기%20위한%20AI%20프롬프트.txt)
- 문서 요구:
  - 부주의형 / 과잉행동·충동형 / 복합형 / 무증상
  - 각 경우에 대한 비교적 길고 공감적인 해석 문단
- 현재 구현:
  - DSM quick analysis는 짧은 `summary / subtype / inattention / hyperactivity / guidance`
  - 문서의 장문 버전은 직접 반영되지 않음
- TODO:
  - DSM quick analysis를 짧게 유지할지
  - 문서형 장문 해석을 report 또는 별도 화면에 넣을지 결정

## 3. 반응성 테스트

### 3-1. 테스트 1 자극 설명이 문서와 완전히 일치하지 않음

- 문서 기준:
  - 목표 자극은 `원`
  - 비목표는 `세모, 네모, 별`
- 현재 구현:
  - `0.gif` ~ `5.gif` 기반의 기존 자산 사용
  - 문서의 도형 명세와 1:1 대응되지는 않음
- TODO:
  - 현재 자산 기준으로 문서를 바꾸거나
  - 자산을 문서 기준 자극으로 교체

### 3-2. 반응성 테스트 2번 자극 자산이 문서와 다름

- 문서 기준:
  - [react/(4-1)반응성 테스트 구조.txt](/home/sqmini/soulai/react/%284-1%29반응성%20테스트%20구조.txt)
  - [반응성게임사양서.md](/home/sqmini/soulai/반응성게임사양서.md)
- 문서 요구:
  - 테스트 2는 `토끼, 고양이, 강아지, 곰` 같은 동물 캐릭터 사용
  - `곰`만 누르지 않는 No-Go 규칙
- 현재 구현:
  - `o.gif`, `x.gif` 자극 사용
  - 동물 캐릭터 구조는 반영되지 않음
- TODO:
  - 테스트 2 자산을 동물 캐릭터 기반으로 교체할지 결정
  - 최소한 문서와 앱 중 하나를 기준으로 통일 필요

### 3-3. 테스트 2 자극 친숙성 근거는 있지만 현재 자산은 추상적임

- 문서 기준:
  - 동물 캐릭터처럼 인식이 쉬운 자극 사용
- 현재 구현:
  - 추상적인 `o/x` 자산 사용
- TODO:
  - 실제 친숙한 자극으로 교체하면 사용자 이탈/혼동 감소 가능성 검토

### 3-4. 테스트 사이 `휴식 5초` 흐름 미구현

- 문서 기준:
  - [react/(4-1)반응성 테스트 구조.txt](/home/sqmini/soulai/react/%284-1%29반응성%20테스트%20구조.txt)
- 문서 요구:
  - 테스트 1 후 5초 휴식
  - 테스트 2 후 5초 휴식
  - 응원 메시지 포함
- 현재 구현:
  - 테스트가 끝나면 바로 다음 테스트 카운트다운으로 넘어감
- TODO:
  - 테스트 사이에 쉬는 화면 추가
  - 메시지와 진행 맥락 분리

### 3-5. 테스트 1, 2 연습 단계 피드백 규칙이 문서와 완전 일치하지 않음

- 문서 기준:
  - 연습 단계에서 시각 피드백 제공
- 현재 구현:
  - 내부적으로 판정은 하지만 현재 화면 오버레이는 숨김 처리됨
- TODO:
  - 연습 단계에서만 피드백을 다시 보여줄지 검토
  - 검사 단계는 숨기고 연습 단계만 보여주는 분기 가능

### 3-6. 테스트 2의 입력 규칙 해석이 문서와 애매하게 남아 있음

- 문서 기준:
  - 전형적인 Go/No-Go 억제 과제
- 현재 구현:
  - 자극이 사라진 뒤에도 같은 시행 입력 허용
  - UI로 그 점을 표시하도록 바뀌었지만, 문서상 "자극이 보일 때 누른다"로 읽힐 수 있음
- TODO:
  - 현재 UX를 공식 규칙으로 문서에 더 명확히 반영
  - 또는 2번만 자극 표시 중 입력으로 좁힐지 재검토

### 3-7. 테스트 무효 시 재시행 플로우 미구현

- 문서 기준:
  - [react/(4-1)반응성 테스트 구조.txt](/home/sqmini/soulai/react/%284-1%29반응성%20테스트%20구조.txt)
- 문서 요구:
  - 테스트 1에서 hit rate가 너무 낮으면 재시행 1회 허용
  - 테스트 2에서도 유효성 낮을 때 세션 무효 판단
- 현재 구현:
  - `validity` 값은 저장되지만
  - 사용자가 바로 재시행하도록 안내하는 UI 흐름은 없음
- TODO:
  - 무효 판정 시 결과 화면에서 재시행 버튼 제공
  - 재시행 1회 제한 규칙 반영

### 3-8. 안티-게이밍 규칙 대부분 미구현

- 문서 기준:
  - [react/(4-1)반응성 테스트 구조.txt](/home/sqmini/soulai/react/%284-1%29반응성%20테스트%20구조.txt)
- 문서 요구:
  - 연타 감지
  - 무반응 감지
  - 패턴 반응 감지
  - 전략적 반응 태깅
- 현재 구현:
  - `anticipatory` 분리와 기본 validity만 있음
  - 문서 수준의 안티-게이밍 탐지는 없음
- TODO:
  - 연속 초고속 탭 감지
  - 연속 무반응 알림
  - 전략적 지연 반응 태깅

### 3-9. 테스트 3 센서 미지원 시 `건너뜀` 규칙과 현재 fallback 구조가 다름

- 문서 기준:
  - [react/(4-1)반응성 테스트 구조.txt](/home/sqmini/soulai/react/%284-1%29반응성%20테스트%20구조.txt)
- 문서 요구:
  - 자이로 미지원 기기면 `test3_valid: false`로 건너뜀
- 현재 구현:
  - 센서가 없으면 포인터/터치 fallback으로 계속 진행
- TODO:
  - 현재 fallback 유지가 맞는지 문서 수정이 맞는지 결정
  - 문서 기준을 따를 경우 센서 미지원 시 스킵 로직 필요

### 3-10. 테스트 3의 바닥 방치 감지 미구현

- 문서 기준:
  - 3축 모두 거의 0이 5초 이상 지속되면 무효
- 현재 구현:
  - `raw_samples`와 기본 샘플 수 검사만 있음
  - 바닥에 내려놓은 상황 감지 없음
- TODO:
  - 저변동 장시간 지속 패턴 검사 추가

### 3-11. 테스트 3 설명은 `정적 유지`인데 fallback은 `움직이는 목표 추적` 요소가 강함

- 문서 기준:
  - "가만히 유지"가 핵심
- 현재 구현:
  - 센서 없을 때는 움직이는 목표를 따라가는 구조
- TODO:
  - fallback도 더 정적 유지형으로 바꿀지 검토

### 3-12. 완료 보상/칭찬 애니메이션이 문서 수준으로 구현되진 않음

- 문서 기준:
  - 완료 시 칭찬 애니메이션 + 응원 문구
- 현재 구현:
  - 결과 화면 텍스트 중심
- TODO:
  - 결과 화면에 보상성 시각 연출 추가 검토

## 4. Report

### 4-1. report 문서의 5블록 구조는 구현됐지만 일부 세부 요구는 축소 또는 확장 반영됨

- 문서 기준:
  - [report/[5-1] 통합 리포트 화면 구조.txt](/home/sqmini/soulai/report/%5B5-1%5D%20통합%20리포트%20화면%20구조.txt)
- 문서 요구:
  - 5블록 구조
  - 탭 1은 ASRS 부주의 + 누락 오류 + 반응시간 변동성
  - 탭 2는 ASRS 충동성 + 오경보 오류
  - 하단 고정 버튼
- 현재 구현:
  - 5블록 구조는 대체로 반영됨
  - 하단 CTA도 있음
  - 다만 현재는 문서보다 더 확장되어 `Tau`, `후반부 정확도 저하`, `성급 반응`, `Go 반응시간`, `반응 패턴`까지 표시
- TODO:
  - 현재 확장 구조를 공식 문서로 승격할지
  - 문서의 단순 구조와 앱의 확장 구조를 통일할지 결정

### 4-2. report 프롬프트 문서상 LLM 중심 서술과 현재 deterministic report 구조 차이

- 문서 기준:
  - [report/[5-2] 통합 리포트 프롬프트.txt](/home/sqmini/soulai/report/%5B5-2%5D%20통합%20리포트%20프롬프트.txt)
- 문서 요구:
  - 리포트 텍스트를 LLM이 생성
- 현재 구현:
  - report 본문은 서버 deterministic 생성이 우선
  - Gemini는 주로 plan 문장 보조 및 일부 quick analysis에 사용
- TODO:
  - 현재 deterministic 구조 유지가 맞는지
  - report 문서를 실제 구현 방식에 맞게 갱신할지 결정

### 4-3. report 프롬프트 문서의 `[5. 행동 수정 실천 계획]`은 report 본문에 직접 구현되지 않음

- 문서 기준:
  - [report/[5-2] 통합 리포트 프롬프트.txt](/home/sqmini/soulai/report/%5B5-2%5D%20통합%20리포트%20프롬프트.txt)
- 문서 요구:
  - report 출력 항목 안에 `행동 수정 실천 계획`
- 현재 구현:
  - 실행 계획은 report 본문이 아니라 `plan` 단계로 분리됨
- TODO:
  - 문서 기준으로 report 안에 짧은 계획 요약을 넣을지
  - 현재처럼 `plan`으로 분리한 구조를 공식 기준으로 문서 수정할지 결정

## 5. 공통 구조 / 데이터 / AI

### 5-1. 반응성 테스트 전용 LLM 요약 파이프라인 미구현

- 문서 기준:
  - [react/(4-3)반응성 테스트 프롬프트.txt](/home/sqmini/soulai/react/%284-3%29반응성%20테스트%20프롬프트.txt)
- 문서 요구:
  - 반응성 테스트 전용 요약 JSON 생성
  - `selected_templates`
  - `cross_check_hint`
  - `game_result` 출력 구조
- 현재 구현:
  - report는 서버 deterministic 문구 중심
  - 반응성 테스트 전용 JSON -> LLM -> game_result 흐름은 따로 없음
- TODO:
  - 게임 결과 전용 API를 만들지
  - 현재 report에 흡수할지 구조 결정 필요

### 5-2. `test1_valid`, `test2_valid`, `test3_valid` 같은 상위 validity 요약 구조 미정리

- 문서 기준:
  - [react/(4-3)반응성 테스트 프롬프트.txt](/home/sqmini/soulai/react/%284-3%29반응성%20테스트%20프롬프트.txt)
- 문서 요구:
  - `test_validity`에 테스트별 유효성 요약
- 현재 구현:
  - 각 테스트 안에 `validity` 객체는 저장
  - 상위 요약 `test_validity` 구조는 별도로 없음
- TODO:
  - report/AI용 통합 JSON 만들 때 상위 validity 필드로 재정리

### 5-3. 반응성 테스트 전용 `selected_templates` 미구현

- 문서 기준:
  - [react/(4-3)반응성 테스트 프롬프트.txt](/home/sqmini/soulai/react/%284-3%29반응성%20테스트%20프롬프트.txt)
- 문서 요구:
  - level에 따라 서버에서 템플릿 문구를 미리 고르고 LLM에 전달
- 현재 구현:
  - report는 deterministic 문구 생성
  - 템플릿 테이블을 별도 상수로 두고 전달하지는 않음
- TODO:
  - 전용 템플릿 상수 분리 여부 결정

### 5-4. 반응성 테스트 전용 `cross_check_hint`는 아직 별도 필드가 없음

- 문서 기준:
  - [react/(4-3)반응성 테스트 프롬프트.txt](/home/sqmini/soulai/react/%284-3%29반응성%20테스트%20프롬프트.txt)
- 문서 요구:
  - LLM 결과 안에 `cross_check_hint`
- 현재 구현:
  - report 본문 안에서 비슷한 역할을 함
  - 독립 필드는 없음
- TODO:
  - 필요하면 report schema에 독립 필드 추가

### 5-5. 과일/동물/도형 등 자극 자산 규칙과 실제 리소스 폴더가 따로 놀고 있음

- 문서 기준:
  - 테스트별 자극 종류가 비교적 구체적으로 적혀 있음
- 현재 구현:
  - 기존 `react1`, `react2` 자산을 최대한 유지하는 방향
- TODO:
  - "문서를 현재 자산에 맞춘다" 또는 "자산을 문서에 맞춘다" 중 하나 선택

## 정리

현재 TODO는 아래 흐름으로 보면 됩니다.

1. ASRS: quick analysis 확장 여부, 영역 툴팁, 예시 분기
2. DSM-5: 시작 안내, 문서 연령 기준 정리, 장문 해석 반영 여부
3. 반응성 테스트: 자산, 휴식, 재시행, 안티-게이밍, fallback 정리
4. report: 문서와 실제 구현 차이 통일
5. 공통 구조: 전용 LLM JSON / validity / template / hint 구조 정리
